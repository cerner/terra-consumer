// TODO: Move this file into terra-consumer-components when available
// import React from 'react';
// import PropTypes from 'prop-types';
// import classNames from 'classnames/bind';
// import styles from './TerraSlide.scss';
//
// const cx = classNames.bind(styles);
//
// const propTypes = {
//   slideContent: PropTypes.element,
//   mainContent: PropTypes.element,
// };

// NOTE: I'm pretty sure we could use terra-slide-panel(with some changes) if the current implementation of keyframes needs changed.
// Currently, terra slide panel's background is not configurable and is always set to white.
// - https://github.com/cerner/terra-core/blob/master/packages/terra-slide-panel/src/SlidePanel.scss#L12
// We would need to add two props to allow for custom classes to manipulate the mainContent and panelContent as well as allowing
// variable theming of background.
